<section id="hero" class="flex items-center justify-between w-full">
    <div class="flex flex-col w-2/5 items-start gap-8">
        <h1 class="text-teal-200 text-6xl capitalize font-bold">
            <span class="text-teal-400"> not</span>
            your ordinary
            <span class="text-teal-400 underline underline-offset-4">
                QR Code</span
            >
            Generator.
        </h1>
        <p class="text-teal-200 font-light text-2xl">
            It is a feature-rich tool designed to simplify the generation of QR
            codes for various purposes.
        </p>
        <div class="flex gap-8">
            <a
                href="#features"
                class="btn cursor-pointer capitalize bg-teal-400 text-gray-950 text-lg font-medium px-8 py-2 rounded-sm hover:bg-teal-600"
            >
                know more
            </a>
            <a
                href="/play"
                class="btn cursor-pointer capitalize border-b border-teal-400 text-teal-400 text-lg font-medium px-8 py-2 rounded-sm hover:bg-gray-900"
            >
                try it out !!
            </a>
        </div>
    </div>
    <div class="hero-image">
        <img
            class="animate-spin-real-slow duration-75"
            src="/hero.png"
            alt="hero qr"
        />
    </div>
</section>

<style>
    #hero > .hero-image {
        @apply relative;
        width: 60px;
    }
    #hero > .hero-image > img {
        @apply absolute top-0;
        left: -4rem;
        max-width: 350px;
        height: 350px;
        object-fit: contain;
    }
    @media screen and (max-width: 620px) {
        #hero {
            @apply items-start;
        }
        #hero h1 {
            @apply text-4xl w-4/5;
        }
        #hero p {
            @apply text-xl;
        }
        #hero > div {
            @apply w-full;
            flex: 1;
        }
        #hero > div > div > a {
            @apply text-sm px-4 py-1;
        }
        #hero > .hero-image > img {
            left: 0;
            max-width: 200px;
            height: 200px;
        }
    }
</style>
